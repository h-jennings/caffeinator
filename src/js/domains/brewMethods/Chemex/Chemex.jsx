import React from 'react';
import PropTypes from 'prop-types';

function Chemex() {
  return (
    <div>
      Chemex Page.
    </div>
  );
}

Chemex.propTypes = {
  // bla: PropTypes.string,
};

Chemex.defaultProps = {
  // bla: 'test',
};

export default Chemex;
