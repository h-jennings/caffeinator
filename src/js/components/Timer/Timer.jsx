import React from 'react';
import PropTypes from 'prop-types';

function Timer() {
  return (
    <div>UI</div>
  );
}
Timer.propTypes = {
  // bla: PropTypes.string,
};

Timer.defaultProps = {
  // bla: 'test',
};

export default Timer;
